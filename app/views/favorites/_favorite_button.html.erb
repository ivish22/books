<% if current_user.favorited?(book) %>
    <%= link_to "Unfavorite", favorites_path(book_id: book.id),method: :delete, class: 'btn btn-info btn-sm' %>
 <% else %>
    <%= link_to "Favorite", favorites_path(book_id: book.id),method: :create, class: 'btn btn-success btn-sm' %>
 <% end %>